this metric uses "outside" libraries : weka.jar libsvm.jar that is why the in manifest file this libraries have been added to the class path and the make install has been changed.
also this metric needs the "classifier.svm" and "dataset.arff" files to work and thus those files have been added in the make install 
