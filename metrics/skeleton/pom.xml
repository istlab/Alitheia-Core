<project>
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>eu.sqooss</groupId>
    <artifactId>metrics</artifactId>
    <version>1.0.0</version>
  </parent>

  <groupId>eu.sqooss.metrics</groupId>
  <artifactId>skeleton</artifactId>
  <version>0.0.1</version>
  <packaging>jar</packaging>

  <name>Alitheia Plug-in 'skeleton'</name>
  <url>http://ikaria.dmst.aueb.gr/metrics/skeleton</url>
  <inceptionYear>2008</inceptionYear>
  <description>
    This metric is a 'no-op' type of metric, intended to function 
    as the basis for plug-in development. Potential developers
    are encouraged to svn cp this metric code to their custom 
    namespace.
  </description>

  <scm>
    <connection>
      scm:svn:https://svn.sqo-oss.eu/devel/trunk/metrics/skeleton
    </connection>
    <developerConnection>
      scm:svn:https://svn.sqo-oss.eu/devel/trunk/metrics/skeleton
    </developerConnection>
  </scm>

  <build>
    <defaultGoal>package</defaultGoal>
    <sourceDirectory>${basedir}/src</sourceDirectory>
    <outputDirectory>${basedir}/build</outputDirectory>
    <plugins>
      <plugin>
        <artifactId>maven-jar-plugin</artifactId>
        <configuration>
          <finalName>${groupId}.${artifactId}-${version}</finalName>
          <archive>
            <manifestFile>${basedir}/manifest/manifest.mf</manifestFile>
            <addMavenDescriptor>false</addMavenDescriptor>
          </archive>
        </configuration> 
      </plugin>
      <plugin>
        <artifactId>maven-deploy-plugin</artifactId>
        <configuration>
          <generatePom>false</generatePom>
        </configuration>
      </plugin>
    </plugins>
  </build>

</project>

