<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

#if( !$symbolicName )
#set( $symbolicName = "${groupId}.${artifactId}" )
#end
#set( $dollar = "$" )

  <modelVersion>4.0.0</modelVersion>
  <groupId>${groupId}</groupId>
  <artifactId>${artifactId}</artifactId>
  <version>${version}</version>
  
  <packaging>bundle</packaging>
  
  <name> ${symbolicName} plugin (${version})</name>
  
  <dependencies>
    <dependency>
      <groupId>junit</groupId>
      <artifactId>junit</artifactId>
      <version>3.8.1</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>eu.sqooss.alitheia</groupId>
      <artifactId>core</artifactId>
      <version>0.9</version>
    </dependency>
    <dependency>
      <groupId>org.osgi</groupId>
      <artifactId>org.osgi.core</artifactId>
      <version>4.2.0</version>
      <optional>true</optional>
    </dependency>
    <dependency>
      <groupId>org.osgi</groupId>
      <artifactId>org.osgi.compendium</artifactId>
      <version>4.2.0</version>
      <optional>true</optional>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>    
        <groupId>org.apache.felix</groupId>
        <artifactId>maven-bundle-plugin</artifactId>
        <extensions>true</extensions>
        <configuration>
          <instructions>
            <Export-Package>${groupId}.${artifactId}</Export-Package>
            <Bundle-Activator>${groupId}.${artifactId}.Activator</Bundle-Activator>
            <Bundle-SymbolicName>${symbolicName}</Bundle-SymbolicName>
            <Bundle-Version>${version}</Bundle-Version>
            <Bundle-Vendor>${groupId}</Bundle-Vendor>
            <Bundle-Description>${symbolicName} SQO-OSS plugin</Bundle-Description>
          </instructions>
        </configuration>
      </plugin>
    </plugins>

  <!--
    Hibernate mapping files are called *.hbm.xml and by convention live
    next to the Java files they belong to; we have the additional
    convention that the private database table sources (Java and
    Hibernate) live in a subdirectory and sub-package db, so we get

    src/eu/sqooss/metrics/<foo>/db 
    You might want to lay things out differently.
  -->
    <resources>
      <resource>
        <targetPath>eu/sqooss/service/db</targetPath>
        <directory>src/main/java/${dollar}{groupId}/${dollar}{artifactId}/db</directory>
        <includes>
          <include>**/*.xml</include>
        </includes>
      </resource>
      <resource>
        <directory>src/main/resources</directory>
        <includes>
          <include>**/*</include>
        </includes>
      </resource>
    </resources>
  </build>
</project>
